<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="menu">
	
	<!-- 메뉴 등록 -->
	<!-- MENU 테이블 -->
	<insert id="menuInsert" parameterType="Menu">
		insert into menu(menu_name,menu_writer,menu_category,menu_tag,menu_reqMaterial,menu_needlessMaterial,
		menu_description,menu_kids,menu_image,menu_totalTime)
		values(#{menu_name},#{menu_writer},#{menu_category},#{menu_tag},#{menu_reqMaterial},
		#{menu_needlessMaterial},#{menu_description},#{menu_kids},#{menu_image},#{menu_totalTime})
	</insert>
	
	<!-- menu_no 찾아오기 -->
	<select id="findNoByName" parameterType="java.lang.String" resultType="java.lang.Integer">
		select menu_no
		from menu
		where menu_name=#{menu_name}
	</select>
	
	<!-- 메뉴 스탯 테이블에 메뉴 번호 등록 -->
	<insert id="menuStatInsert" parameterType="java.lang.Integer">
		insert into menu_stat(menu_no) values(#{menu_no})
	</insert>

	<!-- 레시피 등록 -->
	<insert id="recipeInsert" parameterType="Recipe">
		insert into recipe(menu_no,recipe_processNo,recipe_action,recipe_fire,recipe_reqTime,recipe_image)
		values(#{menu_no},#{recipe_processNo},#{recipe_action},#{recipe_fire},#{recipe_reqTime},#{recipe_image})
	</insert>
	
	<!-- 전체 메뉴 조회 -->
	<select id="menuList" resultType="Menu">
		select m1.menu_no,m1.menu_name,m1.menu_image,m1.menu_description,m1.menu_reqMaterial,m2.menu_star,
		m1.menu_tag,m1.menu_totalTime,m1.menu_writer,m2.menu_preferCnt,m2.menu_readCnt
		from menu m1,menu_stat m2
		where m1.menu_no=m2.menu_no and m1.menu_delete=0
	</select>
	
</mapper>